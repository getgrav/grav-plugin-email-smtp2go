{
    "name": "getgrav/email-smtp2go-tests",
    "description": "Test harness for the Email SMTP2GO plugin. Kept out of the plugin's own composer.json so that the vendor directory shipped with the plugin stays free of development packages.",
    "type": "project",
    "license": "MIT",
    "require-dev": {
        "phpunit/phpunit": "^11.5",
        "symfony/mailer": "^5.4",
        "symfony/mime": "^5.4",
        "symfony/http-client": "^5.4"
    },
    "autoload-dev": {
        "psr-4": {
            "Grav\\Plugin\\EmailSmtp2go\\": "../classes/",
            "Grav\\Plugin\\EmailSmtp2go\\Tests\\": "./"
        }
    },
    "config": {
        "sort-packages": true
    }
}
